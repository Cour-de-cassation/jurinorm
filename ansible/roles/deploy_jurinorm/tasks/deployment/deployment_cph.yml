---
- name: Create Deployment CPH batch
  k8s:
    apply: true
    state: present
    verify_ssl: true
    definition:
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: "{{ cph_batch_app_id }}-deployment"
        namespace: "{{ jurinorm_namespace }}"
        labels:
          app: "{{ cph_batch_app_id }}"
      spec:
        replicas: 1
        selector:
          matchLabels:
            app: "{{ cph_batch_app_id }}"
        template:
          metadata:
            labels:
              app: "{{ cph_batch_app_id }}"
          spec:
            containers:
              - name: "{{ cph_batch_app_id }}"
                image: "{{ cph_batch_image }}"
                envFrom:
                  - configMapRef:
                      name: "batch-config"
                  - secretRef:
                      name: "batch-secret"
...
